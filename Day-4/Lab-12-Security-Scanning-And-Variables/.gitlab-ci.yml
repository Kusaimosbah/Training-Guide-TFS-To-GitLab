# ============================================
# GitLab CI/CD Pipeline for Lab 12
# Security Scanning (SAST + DAST) & Variables
# ============================================
# This pipeline demonstrates:
# 1. GitLab Variables (global, job-level, masked, protected)
# 2. SAST scanning with SonarCloud (static code analysis)
# 3. Dependency scanning with Snyk
# 4. Secret scanning to prevent leaks
# 5. DAST with OWASP ZAP (dynamic app security testing)
# ============================================

stages:
  - build
  - test
  - sast              # Static Application Security Testing
  - dependency        # Dependency vulnerability scanning
  - secret            # Detect accidentally committed secrets
  - deploy            # Deploy to staging
  - dast              # Dynamic Application Security Testing
  - report            # Security summary report

# ============================================
# GLOBAL VARIABLES - Available to all jobs
# ============================================
# These demonstrate variable usage patterns
variables:
  # Configuration variables (not secrets)
  DOTNET_VERSION: "8.0"
  BUILD_CONFIG: "Release"
  PROJECT_NAME: "CapstoneApi"
  ARTIFACT_RETENTION: "7 days"
  LOG_LEVEL: "INFO"
  
  # Security tool configurations
  SONAR_HOST_URL: "https://sonarcloud.io"      # Public SonarCloud URL
  # SONAR_TOKEN: Set in Settings → CI/CD → Variables (masked + protected)
  # SNYK_TOKEN: Set in Settings → CI/CD → Variables (masked + protected)
  
  # DAST configuration
  DAST_ENABLED: "true"
  DAST_TIMEOUT: "1800"                         # 30 minutes
  
  # Environment URLs
  STAGING_URL: "https://staging.example.com"
  
  # GitLab built-in variable override for shallow clone
  GIT_DEPTH: "0"                               # Full clone for better analysis

# ============================================
# BUILD JOB - Compile application
# ============================================
build:
  stage: build
  image: mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION}
  variables:
    # Job-level variables (only in this job)
    BUILD_OUTPUT: "build-output"
  script:
    - echo "🔨 Building $PROJECT_NAME with .NET $DOTNET_VERSION"
    - echo "Configuration: $BUILD_CONFIG"
    
    - dotnet restore src/CapstoneApi/CapstoneApi.csproj
    - dotnet build src/CapstoneApi/CapstoneApi.csproj --configuration $BUILD_CONFIG
    - dotnet publish src/CapstoneApi/CapstoneApi.csproj --configuration $BUILD_CONFIG --output $BUILD_OUTPUT
    
    - echo "✅ Build completed successfully!"
  artifacts:
    paths:
      - $BUILD_OUTPUT/
    expire_in: $ARTIFACT_RETENTION
  only:
    - main
    - merge_requests
    - branches

# ============================================
# TEST JOBS - Run unit and integration tests
# ============================================
test:unit:
  stage: test
  image: mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION}
  variables:
    TEST_FILTER: "Unit"           # Job-level variable
  script:
    - echo "🧪 Running $TEST_FILTER tests..."
    - dotnet test tests/CapstoneApi.Tests/CapstoneApi.Tests.csproj --filter "FullyQualifiedName~${TEST_FILTER}" --configuration $BUILD_CONFIG
  dependencies:
    - build
  only:
    - main
    - merge_requests
    - branches

test:integration:
  stage: test
  image: mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION}
  variables:
    TEST_FILTER: "Integration"    # Different value, same variable name
  script:
    - echo "🧪 Running $TEST_FILTER tests..."
    - dotnet test tests/CapstoneApi.Tests/CapstoneApi.Tests.csproj --filter "FullyQualifiedName~${TEST_FILTER}" --configuration $BUILD_CONFIG
  dependencies:
    - build
  only:
    - main
    - merge_requests
    - branches

# ============================================
# SAST JOB - Static code analysis with SonarCloud
# ============================================
sast:sonarcloud:
  stage: sast
  image: mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION}
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - echo "🔍 Starting SonarCloud SAST (Static) Analysis..."
    - echo "Project: $CI_PROJECT_NAME"
    - echo "Branch: $CI_COMMIT_REF_NAME"
    - echo "Commit: $CI_COMMIT_SHORT_SHA"
    
    - dotnet tool install --global dotnet-sonarscanner
    - export PATH="$PATH:$HOME/.dotnet/tools"
    
    - apt-get update && apt-get install -y openjdk-17-jre-headless
    
    - |
      dotnet sonarscanner begin \
        /k:"capstone-api" \
        /o:"your-organization-key" \
        /d:sonar.host.url="${SONAR_HOST_URL}" \
        /d:sonar.token="${SONAR_TOKEN}"
    
    - dotnet build src/CapstoneApi/CapstoneApi.csproj --configuration $BUILD_CONFIG
    - dotnet sonarscanner end /d:sonar.token="${SONAR_TOKEN}"
    
    - echo "✅ SonarCloud analysis uploaded!"
  dependencies:
    - build
  allow_failure: false
  only:
    - main
    - merge_requests

# ============================================
# DEPENDENCY SCANNING - Detect vulnerable NuGet packages
# ============================================
dependency_scanning:dotnet-audit:
  stage: dependency
  image: mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION}
  script:
    - echo "📦 Scanning dependencies for vulnerabilities..."
    
    # Install dotnet-audit tool for NuGet dependency scanning
    - dotnet tool install --global dotnet-audit || dotnet tool update --global dotnet-audit
    - export PATH="$PATH:$HOME/.dotnet/tools"
    
    # Scan for vulnerable NuGet packages in CapstoneApi
    - echo "Auditing src/CapstoneApi/CapstoneApi.csproj..."
    - dotnet audit src/CapstoneApi/CapstoneApi.csproj --json > dependency-report.json || true
    
    - echo "✅ Dependency scan complete!"
    - cat dependency-report.json | head -50
  artifacts:
    paths:
      - dependency-report.json
    expire_in: 30 days
  allow_failure: true              # Report but don't fail pipeline
  only:
    - main
    - merge_requests

# ============================================
# SECRET SCANNING - Detect leaked credentials
# ============================================
secret_scanning:trufflehog:
  stage: secret
  image: python:3.9
  script:
    - echo "🔐 Scanning git history for secrets..."
    
    - pip install truffleHog3 --quiet
    - trufflehog git file://. --json > secret-report.json || true
    
    - echo "⚠️  Any secrets found must be revoked immediately!"
    - cat secret-report.json | head -20  # Show first 20 lines
  artifacts:
    paths:
      - secret-report.json
    expire_in: 30 days
  allow_failure: true               # Alert but don't block
  only:
    - main
    - merge_requests

# ============================================
# DEPLOY:STAGING - Deploy to staging environment
# ============================================
# Note: This is simplified. Real deployment uses scripts or tools.
deploy:staging:
  stage: deploy
  image: mcr.microsoft.com/dotnet/sdk:${DOTNET_VERSION}
  variables:
    DEPLOY_ENV: "staging"
    DEPLOY_TIMEOUT: "300"
  environment:
    name: staging
    url: https://staging.example.com
  script:
    - echo "🚀 Deploying to $DEPLOY_ENV environment..."
    - echo "Timeout: $DEPLOY_TIMEOUT seconds"
    - echo "Built with: $DOTNET_VERSION"
    
    # In real world, use PowerShell/Bash scripts:
    # - powershell deploy.ps1 -Environment $DEPLOY_ENV -Version $CI_COMMIT_SHORT_SHA
    - echo "✅ Deployment to $DEPLOY_ENV completed!"
  dependencies:
    - build
    - test:unit
    - test:integration
    - sast:sonarcloud
  allow_failure: false
  only:
    - main

# ============================================
# DAST JOB - Dynamic app security testing with OWASP ZAP
# ============================================
# DAST tests the RUNNING application
# NOTE: In production, requires staging deployment to be running first
dast:owasp-zap-demo:
  stage: dast
  image: alpine:latest
  variables:
    ZAP_REPORT_FILE: "zap-report.xml"
  script:
    - echo "🔓 OWASP ZAP DAST (Dynamic) Security Test"
    - echo "NOTE: This is a demonstration/simulated report"
    - echo "In production, this would attack a running staging server at: https://${STAGING_URL}"
    
    # Create example DAST report showing what findings look like
    - |
      cat > $ZAP_REPORT_FILE << 'EOF'
      <?xml version="1.0" encoding="UTF-8"?>
      <OWASPZAPReport version="2.10.0">
        <site name="https://staging.example.com">
          <alerts>
            <alertitem>
              <pluginid>10002</pluginid>
              <alert>X-Frame-Options Header Missing</alert>
              <riskcode>2</riskcode>
              <confidence>2</confidence>
              <description>X-Frame-Options header is not set on API responses</description>
              <uri>https://staging.example.com/api/calc/add</uri>
            </alertitem>
          </alerts>
        </site>
      </OWASPZAPReport>
      EOF
    
    - echo "✅ DAST scan complete!"
    - ls -lh "$ZAP_REPORT_FILE"
  artifacts:
    paths:
      - $ZAP_REPORT_FILE
    expire_in: 30 days
  allow_failure: true             # Demo only, don't block pipeline
  only:
    - main
    - merge_requests

# ============================================
# SECURITY REPORT JOB - Summarize all findings
# ============================================
security_report:
  stage: report
  image: alpine:latest
  script:
    - echo ""
    - echo "======================================"
    - echo "📊 SECURITY SCAN SUMMARY"
    - echo "======================================"
    - echo "Pipeline: $CI_PIPELINE_ID"
    - echo "Project: $CI_PROJECT_NAME"
    - echo "Branch: $CI_COMMIT_REF_NAME"
    - echo "Commit: $CI_COMMIT_SHORT_SHA"
    - echo ""
    - echo "✅ SAST (Static Code Analysis):"
    - echo "   Tool: SonarCloud"
    - echo "   Status: See SonarCloud dashboard"
    - echo ""
    - echo "📦 DEPENDENCY SCANNING:"
    - echo "   Tool: dotnet-audit"
    - echo "   Report: dependency-report.json"
    - echo ""
    - echo "🔐 SECRET SCANNING:"
    - echo "   Tool: TruffleHog"
    - echo "   Report: secret-report.json"
    - echo ""
    - echo "🔓 DAST (Dynamic App Testing):"
    - echo "   Tool: OWASP ZAP"
    - echo "   Report: zap-report.xml"
    - echo ""
    - echo "======================================"
    - echo "All security scans complete!"
    - echo "======================================"
  dependencies:
    - dependency_scanning:dotnet-audit
    - secret_scanning:trufflehog
    - dast:owasp-zap-demo
  only:
    - main
    - merge_requests

# ============================================
# NOTES FOR TRAINEES
# ============================================
# 1. GLOBAL VARIABLES:
#    - Available to all jobs
#    - Reduce code duplication
#    - Example: DOTNET_VERSION, BUILD_CONFIG
#
# 2. JOB-LEVEL VARIABLES:
#    - Override global variables in specific jobs
#    - Example: TEST_FILTER changes per job
#
# 3. MASKED VARIABLES (in Settings → CI/CD → Variables):
#    - Hidden in pipeline logs
#    - Example: SONAR_TOKEN, SNYK_TOKEN
#    - Set flag: ✅ Masked
#
# 4. PROTECTED VARIABLES:
#    - Only available on protected branches (main)
#    - Example: Production credentials
#    - Set flag: ✅ Protected
#
# 5. CI PREDEFINED VARIABLES (built-in):
#    - $CI_PROJECT_NAME - Project name
#    - $CI_COMMIT_SHA - Full commit hash
#    - $CI_COMMIT_SHORT_SHA - Short commit hash
#    - $CI_COMMIT_REF_NAME - Branch name
#    - $CI_PIPELINE_ID - Pipeline ID
#    - $CI_JOB_NAME - Current job name
#
# 6. SAST vs DAST:
#    SAST (Static):
#    - Analyzes SOURCE CODE without running
#    - Fast (1-5 minutes)
#    - Examples: SonarCloud, Snyk
#    
#    DAST (Dynamic):
#    - Tests RUNNING APPLICATION
#    - Slow (5-30 minutes)
#    - Examples: OWASP ZAP, Burp Suite
#
# 7. SECURITY BEST PRACTICES:
#    - Always mask secrets
#    - Always protect prod vars
#    - Use both SAST and DAST
#    - Scan dependencies
#    - Detect secret leaks
#    - Fail on critical vulns
